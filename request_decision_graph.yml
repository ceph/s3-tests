start:
    set: {}
    choice:
        - bucket

bucket:
    set:
        urlpath: /{bucket}
    choice:
        - bucket_get
        - bucket_put
        - bucket_delete

bucket_delete:
    set:
        method: DELETE
        query:
            - null
            - policy
            - website
    choice: []

